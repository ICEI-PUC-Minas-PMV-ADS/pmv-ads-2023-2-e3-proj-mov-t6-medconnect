import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 10,
    marginHorizontal: 20,
  },
  input: {
    borderBottomWidth: 1,
    borderColor: '#333',
  },
  mailInputErrors: {
    backgroundColor: '#900',
    color: '#000',
  },
});
